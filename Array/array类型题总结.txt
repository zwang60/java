array类型题总结
一般分这样几类：
1. for loop，通常都比较简单，但是要注意边界条件
2. binary search，也比较简单，注意边界处理
3. dp，这一类可能比较困难，比如maximum subarray sum，要维护两个dp数组，一个保存全局最大，另一个保存局部最大，这种方法很常用，要熟练掌握
4. partition，如sort color，这类解法不是很常用，但是sort的时候也要想到
5. 数组坐标，Spiral Matrix, Spiral Matrix II, Rotate Image, Valid Sudoku, Set Matrix Zeroes
